version     = "0.1.0"
author      = "Softmax"
description = "Snake Royale — four snakes, one grid, simultaneous moves, last one alive."
license     = "MIT"

srcDir = "src"

requires "nim >= 2.2.4"
requires "mummy >= 0.4.7"
requires "whisky >= 0.1.3"
requires "curly >= 1.1.1"
requires "jsony"
